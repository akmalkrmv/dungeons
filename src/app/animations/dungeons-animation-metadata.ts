import { AnimationTriggerMetadata } from '@angular/animations';

export interface IDungeonsAnimationMetadata {
  NAME: string;
  TRIGGER: AnimationTriggerMetadata;
  TRIGGER_NAME: string;
  START: string;
  DONE: string;
}
